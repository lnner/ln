当子节点固定的宽高大于父节点，且父节点使用overflow:auto时会显示滚动条，滚动条会占位置导致子节点排列偏移

margin-left: calc(100vw - 100%);

100vw相对于浏览器的window.innerWidth，是浏览器的内部宽度，注意，滚动条宽度也计算在内！而100%是可用宽度，是不含滚动条的宽度。

通过父子节点的宽高calc计算出滚动条的宽(高)度再反方向偏移滚动条的宽(高)，前提父节点宽(高)不得超出内容（子节点）加滚动条的宽(高) *2 ，即子节点不得大过父节点的宽(高)